// Shared layout constants for the floating tab bar

export const FLOATING_TAB_BAR_HEIGHT = 64;

// Bottom padding scrollable screens should add to their contentContainerStyle
// so the last item clears the floating bar.
// Covers: bar height (64) + bottom gap (16) + max home-indicator (34) + clearance (6)
export const FLOATING_TAB_BAR_INSET = 120;
